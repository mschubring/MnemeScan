package inc.troll.mnemescan.services;

import android.content.SharedPreferences;
import inc.troll.mnemescan.config.SettingKeys;

public class ApplicationSettingService {

	public void loadHomeActivitySettings() {
//		SharedPreferences settings = getSharedPreferences("ScannerSettings",0);
//		scanSwitch.setChecked(settings.getBoolean(SettingKeys.SCAN_SWITCH,false));
//		scanFrequency.setProgress(settings.getInt(SettingKeys.SCAN_FREQUENCY,5));
	}

	public void saveHomeActivitySettings() {
//		SharedPreferences settings = getSharedPreferences("ScannerSettings",0);
//		SharedPreferences.Editor editor = settings.edit();
//		editor.putBoolean(SettingKeys.SCAN_SWITCH, scanSwitch.isChecked());
//		editor.putInt(SettingKeys.SCAN_FREQUENCY, scanFrequency.getProgress());
//		editor.commit();
	}
}
